<% nothing = true %>
<% @appointments.each do |app| %>
  <% if app.date.strftime('%d/%m/%Y') ==  @dates[num] and app.hour == hour %>
    Reservado <br> <%= app.user.email %>
    <%#= link_to home_view_path(id: app.first.id), {data: {toggle: 'modal', target: '#viewModal'} , remote: :true, method: :patch, class: 'btn btn-primary btn-sm'}  do %>
      <i class="fa fa-eye"></i>
    <%# end %>
    <% if app.user == current_user%>
      <%= link_to app, method: :delete, data: { confirm: 'Tem Certeza?' },  remote: :true  do %>
        <i class="bi bi-calendar-x-fill">X</i>
      <% end %>
    <% end %>

    <% nothing = false%>
  <% end %>
<% end %>
<% if nothing %>
  <%#= link_to home_link_path(date_to: @dates[num], hour_to: hour), remote: :true, method: :patch ,data: {toggle: 'modal', target: '#cadModal'}, class: 'btn btn-success' do %>
    <i class="available" >Disponível</i>
  <%# end %>
<% end %>
